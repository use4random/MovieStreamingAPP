{
  "name": "cinepulse-app",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client --legacy-peer-deps && npm run build --prefix client && node -e \"import('fs').then(fs => fs.cpSync('client/dist', '.', {recursive: true}))\"",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "node-cache": "^5.1.2"
  }
}
